<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Transaction History</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
body { font-family: 'Inter', sans-serif; background: #f3f4f6; padding: 40px 20px; }

.top-bar { max-width: 900px; margin: auto; display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; }

.app-title { font-size: 1.8rem; font-weight: 700; }

.login-btn { text-decoration: none; background: #4f46e5; color: white; padding: 10px 18px; border-radius: 12px; font-weight: 600; }
.login-btn:hover { background: #6366f1; }

.history-card {
    max-width: 700px;
    margin: auto;
    background: white;
    padding: 25px;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.1);
}

.history-title { font-size: 1.4rem; font-weight: 600; margin-bottom: 20px; }

.transaction { display: flex; justify-content: space-between; align-items: center; padding: 12px 0; border-bottom: 1px solid #eee; }

.transaction:last-child { border-bottom: none; }

.tx-info { display: flex; flex-direction: column; }

.tx-desc { font-weight: 600; color: #333; }

.tx-date { font-size: 0.8rem; color: #888; }

.amount.plus { font-weight: 700; color: #10b981; }

.amount.minus { font-weight: 700; color: #ef4444; }

</style>
</head>
<body>

<div class="top-bar">
    <div class="app-title">Transaction History</div>
     <a href="/" class="login-btn">Home</a>
<%if(!user){ %>
        <a href="/login" class="login-btn">Login</a>
  <%} else{ %>
        <a href="/logout" class="login-btn">Logout</a>
<%}%>

</div>

<div class="history-card">
    <div class="history-title">Your Transactions</div>
  <% if (expensedata) { %>

    <%expensedata.forEach(data=> { %>
        <div class="transaction">
            <div class="tx-info">
                <span class="tx-desc"><%= data.description %></span>
                <span class="tx-date"><%= data.email %></span>
            </div>
            <span >
                <%= data.amount %>
            </span>
        </div>
    <% }) %>
    <% } else { %>
        <p>No transactions yet.</p>
    <% } %>
     

</div>

</body>
</html>
