<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Add Transaction</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
:root {
    --primary: #6366f1;
    --primary-dark: #4f46e5;
    --glass: rgba(255,255,255,0.15);
}

body {
    font-family: 'Inter', sans-serif;
    background: linear-gradient(135deg, #667eea, #764ba2);
    min-height: 100vh;
    padding: 40px 20px;
}

.top-bar {
    max-width: 900px;
    margin: auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 30px;
    color: white;
}

.app-title { font-size: 1.8rem; font-weight: 700; }

.login-btn { 
    text-decoration: none; 
    background: white; 
    color: var(--primary-dark); 
    padding: 10px 18px; 
    border-radius: 12px; 
    font-weight: 600;
}
.login-btn:hover { background: var(--primary-dark); color: white; }

.card {
    max-width: 500px;
    margin: auto;
    background: var(--glass);
    backdrop-filter: blur(20px);
    border-radius: 20px;
    padding: 30px;
    color: white;
}

.title { font-size: 1.4rem; font-weight: 600; margin-bottom: 20px; }

.form-group { margin-bottom: 18px; }

.form-group label { display: block; margin-bottom: 8px; font-size: 0.9rem; }

.form-group input {
    width: 100%; padding: 12px 14px; border-radius: 12px; border: none; outline: none;
}

.form-group input:focus { box-shadow: 0 0 0 3px rgba(99,102,241,0.4); }

.btn {
    width: 100%; padding: 14px; border-radius: 14px; border: none; background: white; color: var(--primary-dark); font-weight: 600; cursor: pointer; transition: 0.3s ease;
}

.btn:hover { background: var(--primary-dark); color: white; }

.show-history {
    display: block;
    text-align: center;
    margin-top: 20px;
    color: white;
    font-weight: 600;
    text-decoration: none;
    padding: 10px 0;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.2);
    transition: 0.3s;
}

.show-history:hover { background: rgba(255, 255, 255, 0.4); }
</style>
</head>
<body>

<div class="top-bar">
    <div class="app-title">Add Transaction</div>
         <a href="/history" class="login-btn">View-Data</a>
<%if(!user){ %>
        <a href="/login" class="login-btn">Login</a>
  <%} else{ %>
        <a href="/logout" class="login-btn">Logout</a>
<%}%>

</div>

<div class="card">
    <div class="title">Add New Transaction</div>
    <form action="/expense/storeexpense" method="POST">
        <div class="form-group">
            <label>Description</label>
            <input type="text" name="description" required>
        </div>

        <div class="form-group">
            <label>Amount (negative = expense, positive = income)</label>
            <input type="number" step="0.01" name="amount" required>
        </div>

        <div class="form-group">
            <label>Date</label>
            <input type="date" name="date" required>
        </div>

        <button type="submit" class="btn">Add Transaction</button>
    </form>

    <!-- Link to Show Transactions -->
    <a href="/history" class="show-history">View Transaction History</a>
</div>

</body>
</html>
